[package]
name = "{{PLUGIN_ID}}"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# wclap = "0.1"  # Uncomment when available

[profile.release]
lto = true
opt-level = "z"
